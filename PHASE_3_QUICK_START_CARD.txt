═══════════════════════════════════════════════════════════════════════════════
                    PHASE 3: QUICK START REFERENCE CARD
═══════════════════════════════════════════════════════════════════════════════

🎯 GOAL: Deploy database backend for Non-IT company location tracking
⏱️  TIME: ~5 minutes total

───────────────────────────────────────────────────────────────────────────────
✅ STEP 1: APPLY DATABASE MIGRATION (2 minutes)
───────────────────────────────────────────────────────────────────────────────

GO TO: https://supabase.com/dashboard
SELECT: Your project
CLICK: SQL Editor → New Query

PASTE THIS SQL:
───────────────────────────────────────────────────────────────────────────────
ALTER TABLE users ADD COLUMN IF NOT EXISTS company_type VARCHAR(50) DEFAULT 'it';
ALTER TABLE users ADD COLUMN IF NOT EXISTS location_tracking_enabled BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS last_location_ping TIMESTAMP WITH TIME ZONE;

ALTER TABLE companies ADD COLUMN IF NOT EXISTS company_type VARCHAR(50) DEFAULT 'it';
ALTER TABLE companies ADD COLUMN IF NOT EXISTS is_verified BOOLEAN DEFAULT FALSE;
ALTER TABLE companies ADD COLUMN IF NOT EXISTS email_verification_token VARCHAR(255) UNIQUE;
ALTER TABLE companies ADD COLUMN IF NOT EXISTS email_verified_at TIMESTAMP WITH TIME ZONE;

CREATE TABLE IF NOT EXISTS location_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role VARCHAR(50) NOT NULL,
  latitude DECIMAL(10, 8) NOT NULL,
  longitude DECIMAL(11, 8) NOT NULL,
  accuracy DECIMAL(10, 2),
  timestamp TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_location_logs_user_timestamp ON location_logs(user_id, timestamp);
CREATE INDEX IF NOT EXISTS idx_location_logs_timestamp ON location_logs(timestamp);
CREATE INDEX IF NOT EXISTS idx_users_company_type ON users(company_type);

ALTER TABLE location_logs ENABLE ROW LEVEL SECURITY;

CREATE POLICY IF NOT EXISTS "Users can insert their own location logs" ON location_logs
  FOR INSERT WITH CHECK (user_id = auth.uid());

CREATE POLICY IF NOT EXISTS "Users can view location logs from their company" ON location_logs
  FOR SELECT USING (
    user_id IN (
      SELECT id FROM users 
      WHERE company_id IN (
        SELECT company_id FROM users WHERE id = auth.uid()
      )
    )
  );
───────────────────────────────────────────────────────────────────────────────

CLICK: "Run" button
WAIT FOR: "Success" message

✅ DATABASE MIGRATION COMPLETE

───────────────────────────────────────────────────────────────────────────────
✅ STEP 2: UPDATE TEST USER (1 minute)
───────────────────────────────────────────────────────────────────────────────

STILL IN SQL EDITOR, PASTE THIS:
───────────────────────────────────────────────────────────────────────────────
UPDATE users 
SET company_type = 'non-it', location_tracking_enabled = true 
WHERE email = 'giwore2911@dolofan.com';

SELECT email, company_type, location_tracking_enabled 
FROM users 
WHERE email = 'giwore2911@dolofan.com';
───────────────────────────────────────────────────────────────────────────────

CLICK: "Run" button
VERIFY: User shows company_type = 'non-it'

✅ TEST USER UPDATED

───────────────────────────────────────────────────────────────────────────────
✅ STEP 3: START DEVELOPMENT SERVER (1 minute)
───────────────────────────────────────────────────────────────────────────────

OPEN COMMAND PROMPT, RUN:
npm run dev

WAIT FOR: "Local: http://localhost:5173"

✅ SERVER RUNNING

───────────────────────────────────────────────────────────────────────────────
✅ STEP 4: TEST DASHBOARD (1 minute)
───────────────────────────────────────────────────────────────────────────────

OPEN BROWSER: http://localhost:5173

LOGIN WITH:
  Email:    giwore2911@dolofan.com
  Password: password123

CLICK: "Allow" when browser asks for location permission

VERIFY YOU SEE:
  ✅ Profile loads (no errors)
  ✅ Company Type: "Non-IT"
  ✅ Location badge on dashboard
  ✅ Your latitude/longitude showing
  ✅ Green "Location Tracking Active" indicator

✅ DASHBOARD TESTING COMPLETE

═══════════════════════════════════════════════════════════════════════════════
🎉 SUCCESS! You have completed Phase 3 deployment.
═══════════════════════════════════════════════════════════════════════════════

Total Time: ~5 minutes
Status: Non-IT Dashboard is now FULLY FUNCTIONAL ✅

You can now:
✅ Track employee locations in real-time
✅ View location history
✅ See other employees' locations (if HR/Admin)
✅ Export location reports
✅ Test all Non-IT features

═══════════════════════════════════════════════════════════════════════════════
❌ TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

PROBLEM: "Profile fetch error"
SOLUTION: Did you run Step 1? If yes, refresh the page.

PROBLEM: "location_logs table does not exist"
SOLUTION: Paste the full SQL from Step 1 again. Make sure to click Run.

PROBLEM: Browser won't show location
SOLUTION: Click "Allow" in the permission popup. Check Privacy settings.

PROBLEM: "User not authenticated"
SOLUTION: Logout and login again. Check .env file has correct URLs.

PROBLEM: Can't see other users' locations
SOLUTION: That's normal if you're not HR/Admin. Create another user or login as HR.

═══════════════════════════════════════════════════════════════════════════════
📚 DOCUMENTATION
═══════════════════════════════════════════════════════════════════════════════

For more details, read these files:

• PHASE_3_READY.md              ← Overview of Phase 3 setup
• PHASE_3_SETUP_GUIDE.md        ← Detailed step-by-step guide
• PHASE_3_ACTION_CHECKLIST.md   ← Detailed checklist
• IMPLEMENTATION_STATUS.md      ← Complete project status
• NON_IT_IMPLEMENTATION_PLAN.md ← Full architecture document

═══════════════════════════════════════════════════════════════════════════════
✨ YOU'RE DONE! PHASE 3 DEPLOYED SUCCESSFULLY
═══════════════════════════════════════════════════════════════════════════════
